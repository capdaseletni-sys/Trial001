name: 🚀 Update Merged Playlist 📺

on:
  schedule:
    - cron: '0 * * * *'  # ⏰ Every 30 minutes (UTC)
  workflow_dispatch:        # 🔘 Manual trigger

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install required Python dependency
        run: pip3 install requests

      - name: 📦 Install aiohttp
        run: pip3 install aiohttp

      - name: 🎯 Run scraping script
        run: curl -L -A "Mozilla/5.0" -o playlist.m3u8 "http://supersonictv.live:8080/get.php?username=611366&password=682256&&type=m3u_plus"
             
             cat playlist.m3u8

             curl -L -A "Mozilla/5.0" -o playlist1.m3u8 "http://cord-cutter.net:8080/get.php?username=15564292&password=15564292&type=m3u_plus"
             
             cat playlist1.m3u8

             curl -L -A "Mozilla/5.0" -o playlist2.m3u8 "http://www.sansat.plus:88/get.php?username=02060789178359&password=02:00:00:00:00:00&type=m3u"
             
             cat playlist2.m3u8

             curl -L -A "Mozilla/5.0" -o playlist3.m3u8 "http://protv65.shop:8080/get.php?username=hsn3868&password=hsn00xy&type=m3u_plus"
             
             cat playlist3.m3u8

             curl -L -A "Mozilla/5.0" -o playlist4.m3u8 "http://portal-iptv.net:8080get.php?username=077998950140&password=077998950140&&type=m3u_plus"
             
             cat playlist4.m3u8

             python3 supersonic.py supersonic.m3u8 playlist.m3u8 playlist1.m3u8 playlist2.m3u8 playlist3.m3u8 playlist4.m3u8

      - name: 💾 Commit & Safely Push if Playlist Changed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@users.noreply.github.com"

          git add supersonic.m3u8

          if git diff --cached --quiet; then
            echo "✅ No changes to commit"
            exit 0
          fi

          git commit -m "🔁 Update playlist $(date -u +'%a %b %d %T UTC %Y')"

          sleep $((RANDOM % 10 + 5))

          if ! git push origin main; then
            echo "⚠️ Push rejected. Trying safe fetch + rebase..."
            git pull origin main --rebase || {
              echo "❌ Rebase failed. Exiting to avoid corruption."
              exit 1
            }
            git push origin main || {
              echo "❌ Second push failed. Remote changed again."
              exit 1
            }
          fi

     
